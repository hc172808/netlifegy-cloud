# NETLIFEGY Cloud v1

A lightweight private cloud platform with:

- Docker container management
- Traefik reverse proxy
- PostgreSQL, Redis
- MinIO object storage
- Grafana & Prometheus monitoring
- Portainer dashboard

## Install

```bash
chmod +x install.sh
sudo ./install.sh
